export const movieName = 'STATES';
export const movieType = 'Comedy';
export const movieView = '3D';
export const movieRate = '8.5';

export const MAX_SEATS_SELECT = 5;

const MAX_ROWS = 7;
const MAX_COLUMNS = 16;

export const roomSeatsRows = new Array(MAX_ROWS).fill('');
export const roomSeatsColumns = new Array(MAX_COLUMNS).fill('');

export const notInUseSeats = [[5, 0], [5, 1]];

export const seatRowPrice = new Array(MAX_ROWS).fill(20);